<template>
  <DemoPage id="judging">
    <template #intro>
      Card guessing, the ganzfeld and remote viewing score people's responses against targets, and
      every p-value they report is valid only under the randomisation the design actually has. This
      page computes those nulls exactly and lets you break them on purpose: play a real shuffled
      Zener pack and watch the closed-deck SD differ from the binomial one, turn feedback on and see
      chance move from 5 hits to 8.65, count all 720 pairings of an SRI judging matrix, pool
      displacement offsets with the variance the call patterns actually imply, and price the best of
      several looks. The mathematics is exact and checked against published values; whether psi
      exists is a contested hypothesis that nothing here tests.
    </template>
    <JudgingDemo />
  </DemoPage>
</template>
