<template>
  <DemoPage id="scan">
    <template #intro>
      AetherOne's scanning and broadcasting model, re-expressed on unbiased primitives and given the
      one thing it never had: a real null. A race ranks a random subset by energy, General Vitality
      arrives with its exact tail, and every catalog item is scored against a fair-coin chance rate
      of exactly ½ — with Holm, Benjamini–Hochberg and an omnibus test of the source itself. Six
      surfaces below: scan a catalog, watch the null model catch a deliberately broken RNG,
      broadcast a rate and verify its receipt, hash a witness signature into a rate, sweep the
      classical dials against their exact law, and run the pre-registered tripolar protocol with a
      yoked control arm.
    </template>
    <ScanDemo />
  </DemoPage>
</template>
